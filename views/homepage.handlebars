{{!-- <div class="post-card">
    <div class="post-card-header">
        {{#each posts as |post|}}
        <div class="dish col-md-4">
            <p>
                Title:
                {{dbPostData.dataValues.title}}.
            </p>
        </div>
        <div class="col-md-5">
            <p>
                Posted:
                {{dbPostData.dataValues.createdAt}}
            </p>
        </div>
    </div> --}}